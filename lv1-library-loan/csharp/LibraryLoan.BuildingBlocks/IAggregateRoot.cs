namespace LibraryLoan.BuildingBlocks;

/// <summary>
/// 集約ルートであることを示すマーカーインターフェース。
///
/// このインターフェース自体は振る舞いを持たない。役割は2つ:
/// 1. 「外部(アプリケーション層)は集約ルートを経由してのみ内部エンティティを操作してよい」
///    という設計上の制約を、レビュー時にコードから読み取れるようにする。
/// 2. 将来的にリポジトリの型制約(例: IRepository&lt;T&gt; where T : IAggregateRoot)として
///    利用できるようにしておく拡張ポイント。
///
/// 意図的にドメインイベントの蓄積などは持たせていない。
/// それらは別途 IHasDomainEvents のような形で必要になったタイミングで追加する方が、
/// 「使わない機能を持ち込まない」というこのライブラリの方針に合う。
/// </summary>
public interface IAggregateRoot
{
}
